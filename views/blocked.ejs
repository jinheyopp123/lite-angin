<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>차단된 사용자 목록</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }

        h1 {
            margin-bottom: 20px;
        }

        .user-list {
            list-style-type: none;
            padding: 0;
        }

        .user-item {
            margin-bottom: 10px;
            padding: 10px;
            background-color: #f9f9f9;
            border-radius: 5px;
        }

        .user-item span {
            margin-right: 10px;
        }

        .error {
            color: red;
            margin-top: 10px;
        }

        .form-container {
            margin-top: 20px;
        }

        input[type="text"] {
            padding: 5px;
            margin-right: 10px;
        }

        select {
            padding: 5px;
            margin-right: 10px;
        }

        button {
            padding: 5px 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h1>차단된 사용자 목록</h1>
    <ul class="user-list">
        <% users.forEach(user => { %>
        <li class="user-item">
            <span><%= user.username %></span>
        </li>
        <% }); %>
    </ul>

    <% if (errorMessage) { %>
    <div class="error">
        <%= errorMessage %>
    </div>
    <% } %>

    <div class="form-container">
        <form action="/blocked/add" method="post">
            <input type="text" name="username" placeholder="사용자 닉네임">
            <select name="duration">
                <option value="1s">1초</option>
                <option value="2w">2주</option>
                <option value="1m">1달</option>
                <option value="permanent">영구</option>
            </select>
            <button type="submit">추가</button>
        </form>
    </div>
</body>

</html>
